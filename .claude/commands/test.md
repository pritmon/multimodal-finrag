Run the test suite for this project.

```bash
cd "/Users/pritammondal/CLAUDE CODE/multimodal-finrag"
source .venv/bin/activate
pytest tests/test_pdf_parser.py -v --tb=short
```

Report: how many passed, how many failed, and the exact error if any failed.
